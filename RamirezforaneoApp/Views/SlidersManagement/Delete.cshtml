@model RamirezforaneoApp.Models.Slider;

@{
    ViewData["Title"] = "Delete Image";
}

<h1>Delete Slider</h1>

<h3>Are you sure you want to delete this?</h3>

<div>
    <h4>Slider</h4>
    <hr />
    <dl class="row">
        <td>
            @Html.DisplayFor(model =>  model.SliderUrlImage)
        </td>
        <dt class="col-sm-2">
            <img src="@Model.SliderUrlImage" alt="Slider Image" style="max-width: 200px; height: auto;" />
        </dt>
    </dl>

</div>